/*
 * We are declaring the default repositories for all of our other conventions
 *
 * Note:
 * Some developer don't like to have mavenLocal() enabled per default
 */
repositories {
    mavenLocal()
    mavenCentral()
}
